<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.DayNight.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
        <item name="android:windowBackground">@color/white</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="ThemeSpinner">
        <!-- Color when pressed -->
        <item name="colorAccent">@color/colorAccent</item>
        <!-- Default color for the dropdown arrow and line -->
        <item name="colorControlNormal">@color/gray600</item>
    </style>

    <style name="Autocomplete">
        <item name="colorControlNormal">@color/blue</item>
        <item name="colorControlActivated">@color/gray600</item>
    </style>

    <style name="MyDatePickerDialogTheme" parent="Theme.AppCompat.Light.Dialog">
        <!-- this is new -->
        <item name="colorAccent">@color/colorAccent</item>

        <item name="android:datePickerStyle">@style/DefaultDatePicker</item>
        <item name="android:colorAccent" tools:targetApi="lollipop">@color/colorAccent</item>
    </style>


    <!--<style name="DefaultDatePicker" parent="android:Theme.Material.Light.Dialog">-->
        <!--<item name="android:headerBackground">@color/colorPrimary</item>-->
        <!--<item name="android:calendarTextColor">@color/colorPrimaryDark</item>-->
        <!--<item name="android:dayOfWeekBackground">@color/colorPrimaryDark</item>-->
        <!--<item name="android:yearListSelectorColor">@color/colorAccent</item>-->
        <!--<item name="android:datePickerMode">calendar</item>-->
    <!--</style>-->

</resources>
